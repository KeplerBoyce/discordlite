export type UserType = {id: string, name: string, status: string};
export type ChannelType = {id: string, name: string};
export type MessageType = {user: {id: string, name: string}, msg: string, timestamp: string}
